import { styled } from "stitches.config";


export const Container = styled('div', {
  display: 'inline',
  a: {
    textDecoration: 'underline',
    color: '$information',
    fontWeight: 'inherit'
  }
})
