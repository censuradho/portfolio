import { styled } from 'stitches.config'

export const Header = styled('header', {
  width: '100%',
  minHeight: '4.375rem',
  display: 'flex',
  alignItems: 'center',
  padding: '1rem',
  position: 'sticky',
  top: 0,
  zIndex: 10,
  backgroundColor: '$background'
})