import { styled } from 'stitches.config'

export const Header = styled('header', {
  width: '100%',
  minHeight: '4.375rem',
  backdropFilter: 'blur(5px)',
  display: 'flex',
  alignItems: 'center',
  padding: '1rem'
})